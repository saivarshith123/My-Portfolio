import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saivarshith.portfolio',
  appName: 'my-portfolio',
  webDir: 'build'
};

export default config;
